export interface User {
    firstName: string,
    lastName: string,
    email: string,
    accessToken: string
}

export interface UserDto {
    firstName: string,
    lastName: string,
    email: string,
}